1. Create a virtual environment (may be different on windows)

```python3 -m venv .venv```

2. Activate the environment (may be different on windows)

```source .venv/bin/activate```

3. Install relevant packages 

```pip install -r requirements.txt```